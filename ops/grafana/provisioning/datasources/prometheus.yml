# Auto-provisions the Prometheus datasource so the dashboard works on
# first `docker compose --profile observability up` with no manual
# Grafana setup.
apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
